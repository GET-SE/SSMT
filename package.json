{
  "private": true,
  "scripts": {
    "build": "next build",
    "export": "next export",
    "deploy": "npm run build && npm run export && touch out/.nojekyll && gh-pages -d out"
  },
  "dependencies": {
    "next": "^14.0.1",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
